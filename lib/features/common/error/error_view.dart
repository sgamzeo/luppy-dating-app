import 'package:luppy_dating_app/core/base/base_stateless_widget.dart';

class ErrorView extends BaseStatelessWidget {
  const ErrorView({super.key});

  @override
  Widget build(context) {
    return const Scaffold();
  }
}
